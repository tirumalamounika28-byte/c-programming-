#include <stdio.h>
#include <string.h>
#include<ctype.h>
int main()
{
char str[50] = "shiva";
int i;
printf("Original string: %s\n", str);
for (i = 0; str[i] != '\0'; i++)
{
str[i] = toupper(str[i]);
}
printf("Upperrcase string: %s", str);
return 0;
}
