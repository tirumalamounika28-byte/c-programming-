#include<stdio.h>
int main()
{
int n,a[10],pos,i;
printf("Enter size of an arary");
scanf("%d",&n);
printf("Enter array elements");
for(i=0;i<=n;i++)
scanf("%d",&a[i]);
printf("After deleting array elements are");
for(i=0;i<n-1;i++)
printf("%d",&a[i]);
return 0;
}
