#include<stdio.h>
int main()
{
int n,sum=0,r,amstrong;
printf("enter n");
scanf("%d",&n);
amstrong=n;
while(n!=0)
r=n%10;
sum=sum+r*r*r;
n=n /10;
if(amstrong==sum)
printf("Amstrong number");
else
printf("Not an amstrong number");
return 0; 
}
