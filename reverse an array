#include<stdio.h>
int main()
{
int a[5],i,n;
printf("enter size of one element");
scanf("%d",&n);
printf("Enter elements of one array");
for(i=0;i<n;i++)
scanf("%d",&a[i]);
printf("\n after reversing array elements one");
for(i=n-1;i>0;i--);
printf("%d\t",a[i]);
return 0;
}
