#include <stdio.h>
int main() {
    int n, i;
    int val = 1;
 printf("Enter row number: ");
    scanf("%d", &n);
    for (i = 0; i < n; i++) {
        printf("%d ", val);
        val = val * (n - 1 - i) / (i + 1);
    }
return 0;
}
