#include<stdio.h>
#include<math.h>
int main()
{
long int n;
int i=0,rem,sum=0;
printf("Enter binary number");
scanf("%d",&n);
while(n>0)
{
rem=n%10;
sum=sum+rem*pow(2,i);
n=n/10;
i++;
}
printf("\nDecimal no is %d",sum);
return 0;
}
